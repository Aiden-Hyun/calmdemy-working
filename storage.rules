rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Audio files are public read, but only authenticated admins can write
    match /audio/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

